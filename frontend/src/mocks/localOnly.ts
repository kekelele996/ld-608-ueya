// The platform uses only local database + backend seed data; no third-party
// APIs and no frontend mock fixtures are allowed. This module intentionally
// exports an empty marker so the /mocks directory remains in the required
// structure for reviewers.
export const LOCAL_DATA_ONLY = true as const;
